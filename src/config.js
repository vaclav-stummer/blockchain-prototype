/* Mining */
const DIFFICULTY = 4; // Difficulty
const MINE_RATE = 3000; // Rate in milliseconds in which block can be mined
const MINING_REWARD = 50; // Reward for the mined transaction

/* Wallet */
const INITIAL_BALANCE = 500;

module.exports = { DIFFICULTY, MINE_RATE, INITIAL_BALANCE, MINING_REWARD };
