/* Blockchain */
const DIFFICULTY = 4; // Mining difficulty
const MINE_RATE = 3000; // Rate in milliseconds in which block can be mined

/* Wallet */
const INITIAL_BALANCE = 500;

module.exports = { DIFFICULTY, MINE_RATE, INITIAL_BALANCE };
